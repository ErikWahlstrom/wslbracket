export class Surfer {
  name: string;
  rank: number;
  tier: number;
  constructor(name: string, rank: number, tier: number) {
    this.name = name;
    this.rank = rank;
    this.tier = tier;
  }
}
